import styled from 'styled-components';
import { Font, Color, Shadow } from '../../../style/Mixins';

const AuthPasswordReset = styled.a`
	${Font(700, '14px')}
	color: ${Color.Red};
	margin-top: 15px;
`

export default AuthPasswordReset;